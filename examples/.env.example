# Example environment for bridge/dashboard integration
OBS_BRIDGE_URL=http://127.0.0.1:7171
# Optional bearer tokens for local dev (leave unset in trusted local setups)
# BRIDGE_TOKEN=
# DS token for DS service health/capabilities
# DS_TOKEN=
